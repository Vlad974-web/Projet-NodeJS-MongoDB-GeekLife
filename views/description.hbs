<section class="section">
    <div class="container">
        <div class="row">

            <div class="col-md-6">
                {{article.image}}
            </div>

            <div class="col-md-6">
                <h1 class="title">{{article.title}}</h1>
                <p>{{article.description}}</p>
                <h5>Prix:  {{prix}}€</h5>
            </div>
        </div>
    </div>
</section>
